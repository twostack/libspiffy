# https://dart.dev/guides/libraries/private-files
# Created by `dart pub`
.dart_tool/

# Avoid committing pubspec.lock for library packages; see
# https://dart.dev/guides/libraries/private-files#pubspeclock.
pubspec.lock

# `docs` was ignored wholesale, so four of the five guides README links to
# were never in the repository - present on this machine, shipped in the
# published archive, and absent from a fresh clone (release prep, 23 Sep).
# They are tracked now.

*.iml
.idea
.DS_Store
build/
test/cdn/
test/data/cdn/

# Isar database the eventador package creates when a test builds a
# persistence backend without naming a directory. A 1 MB binary rewritten by
# every test run, recreated on demand, referenced by nothing in this repo.
data/eventador.isar
data/eventador.isar.lock

# Claude Code agent worktrees
.claude/worktrees/
